import { NotImplementedClient } from './notImplementedClient';

export class AnthropicClient extends NotImplementedClient {
  constructor(_apiKey?: string) {
    super('anthropic', 'Anthropic Messages API integration is planned.');
  }
}
